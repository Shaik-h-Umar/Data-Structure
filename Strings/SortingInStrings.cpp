#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    // string s = "name";
    // sort(s.begin(),s.end());
    // cout<<s;
    string s = " ";
    int asc = (int)s[0];
    cout<<asc;
    return 0;
}